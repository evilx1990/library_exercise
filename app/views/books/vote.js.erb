// update average rating
$('#rating').html('<%= @book.rating %>')
var range = document.querySelector('range')
range.disabled = true
range.value = <%= @book.rating %>
    console.log(1)
// update votes counter
$('#votes').html('votes count: <%= @book.votes_count %>')
